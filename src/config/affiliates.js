// Zentrale Affiliate-IDs. SOBALD die Booking.com-Affiliate-ID da ist:
// nur BOOKING_AID hier setzen -> alle Booking-Links auf der ganzen Seite (2400+ Artikel-CTAs)
// hängen automatisch ?aid=...&label=... an und verdienen. Leer = aktuelles Verhalten (kein Tracking).
export const BOOKING_AID = '';            // <- hier die Booking aid eintragen (z.B. '1234567')
export const BOOKING_LABEL_PREFIX = 'zercy';
